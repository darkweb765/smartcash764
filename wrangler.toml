name = "smartpay"
compatibility_date = "2025-06-01"

# Static SPA deployment: Cloudflare Workers serves the Vite build output
# and falls back to index.html for client-side routes.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
